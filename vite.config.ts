import { defineConfig } from 'vite'

export default defineConfig({
    // For GitHub Pages: set to '/<repo-name>/'
    // Change 'bus-organizer' to match your GitHub repository name
    base: '/bus-organizer/',
})
